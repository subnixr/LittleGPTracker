-include $(PWD)/rules/base

#STRIP = mipsel-linux-strip
STRIP = ls

#---------------------------------------------------------------------------------
%.dge: $(OFILES)
	$(CXX) $(LDFLAGS) -o $@ $(OFILES) $(LIBS)
	@$(STRIP) $@
