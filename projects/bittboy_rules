-include $(PWD)/base_rules

STRIP = $(DEVKIT)/arm-buildroot-linux-musleabi/bin/strip

#---------------------------------------------------------------------------------
%.elf: $(OFILES)
	$(CXX) $(LDFLAGS) -o $@ $(OFILES) $(LIBS)
	@$(STRIP) $@
